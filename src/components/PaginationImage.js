import './PaginationImage.css';

const PaginationImage = () => {
    return (
        <div className='pagination__div'>
            <button>View more</button>
        </div>
    );
};

export default PaginationImage;